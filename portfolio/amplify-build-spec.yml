version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
  build:
    commands:
      - echo Building the project...
  post_build:
    commands:
      - echo Post build tasks...

artifacts:
  files:
    - '**/*'
  base_directory: build
  